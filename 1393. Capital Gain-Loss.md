Here is the complete compiled markdown documentation featuring all five optimized solutions for the **Capital Gain/Loss** problem across MySQL, MS SQL Server, PostgreSQL, Oracle, and Pandas.

---

# Capital Gain/Loss Solutions

This document contains optimal, benchmark-tested solutions for calculating the net capital gain or loss for each stock. The underlying logic treats stock purchases (`Buy`) as cash outflows (negative values) and sales (`Sell`) as cash inflows (positive values), aggregating them efficiently across different database engines and Pandas.

---

## 1. MySQL

```sql
# Write your MySQL query statement below
SELECT 
    stock_name,
    SUM(CASE WHEN operation = 'Buy' THEN -price ELSE price END) AS capital_gain_loss
FROM 
    Stocks
GROUP BY 
    stock_name;

```

---

## 2. MS SQL Server (T-SQL)

```sql
/* Write your T-SQL query statement below */
SELECT 
    stock_name,
    SUM(CASE WHEN operation = 'Buy' THEN -price ELSE price END) AS capital_gain_loss
FROM 
    Stocks
GROUP BY 
    stock_name;

```

---

## 3. PostgreSQL

### Standard `CASE WHEN` Approach

```sql
-- Write your PostgreSQL query statement below
SELECT 
    stock_name,
    SUM(CASE WHEN operation = 'Buy' THEN -price ELSE price END) AS capital_gain_loss
FROM 
    Stocks
GROUP BY 
    stock_name;

```

### Idiomatic `FILTER` Clause Approach

```sql
-- Alternative PostgreSQL-specific optimized syntax
SELECT 
    stock_name,
    SUM(price) FILTER (WHERE operation = 'Sell') - 
    SUM(price) FILTER (WHERE operation = 'Buy') AS capital_gain_loss
FROM 
    Stocks
GROUP BY 
    stock_name;

```

---

## 4. Oracle PL/SQL

```sql
/* Write your PL/SQL query statement below */
SELECT 
    stock_name,
    SUM(CASE WHEN operation = 'Buy' THEN -price ELSE price END) AS capital_gain_loss
FROM 
    Stocks
GROUP BY 
    stock_name;

```

---

## 5. Pandas (Python)

### Vectorized Performance Approach (Recommended)

```python
import pandas as pd
import numpy as np

def capital_gainloss(stocks: pd.DataFrame) -> pd.DataFrame:
    # Use numpy to quickly negate 'Buy' prices across the whole series
    stocks['price'] = np.where(stocks['operation'] == 'Buy', -stocks['price'], stocks['price'])
    
    # Group by stock_name, sum the prices, and format the output
    return (stocks.groupby('stock_name')['price']
            .sum()
            .reset_index(name='capital_gain_loss'))

```

### Alternative Row-by-Row `apply` Approach

```python
import pandas as pd

def capital_gainloss(stocks: pd.DataFrame) -> pd.DataFrame:
    # Convert 'Buy' prices to negative and keep 'Sell' prices positive
    stocks['price'] = stocks.apply(
        lambda row: -row['price'] if row['operation'] == 'Buy' else row['price'], 
        axis=1
    )
    
    # Group and aggregate
    result = stocks.groupby('stock_name')['price'].sum().reset_index()
    result.columns = ['stock_name', 'capital_gain_loss']
    
    return result

```
